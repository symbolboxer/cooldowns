# Cooldowns

This is a simple cooldown timer. It stores timer data in local storage, so state will persist across sessions, but no data leaves the device.

This was coded by an LLM as part of an experiment. It may not reflect the choices I would make if I were to write it from scratch.
